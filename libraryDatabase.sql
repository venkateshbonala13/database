use library
create table Member(
memberID bigint,
name varchar(30),
email varchar(50),
phone_number bigint
)

create table book(
bookID varchar(30),
title varchar(30),
author varchar(30),
price double
)